# Pinned to the versions this code is known to run on.
#
# Installed into the venv by slurm/env.sh; you should not need to run pip yourself.
#
# torch is deliberately pinned without a +cpu local tag so that this file stays
# platform-neutral. slurm/env.sh installs it from PyTorch's CPU index first, because
# the default PyPI wheel drags in ~2.5GB of CUDA that these CPU-only jobs never touch
# and cluster home quotas are tight. "torch==2.12.1" is satisfied by the "2.12.1+cpu"
# wheel, so the pip install -r below is then a no-op for torch.

pyepo==2.2.7
gurobipy==13.0.2
torch==2.12.1
numpy==1.26.4
scipy==1.17.1
scikit-learn==1.9.0
matplotlib==3.10.8
pathos==0.3.5
tqdm==4.67.3